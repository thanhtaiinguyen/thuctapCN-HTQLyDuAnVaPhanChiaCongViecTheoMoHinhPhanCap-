@model thuctapCN.Models.Project

@{
    ViewData["Title"] = "Xóa dự án";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h3 class="card-title mb-0"><i class="bi bi-exclamation-triangle"></i> Xác nhận xóa dự án</h3>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-circle"></i> Cảnh báo: Hành động này không thể hoàn tác. Tất cả dữ liệu liên quan đến dự án này (công việc, thành viên, tài liệu...) sẽ bị xóa vĩnh viễn.
                    </div>

                    <dl class="row">
                        <dt class="col-sm-3">Mã dự án</dt>
                        <dd class="col-sm-9"><span class="badge bg-secondary">@Model.ProjectCode</span></dd>

                        <dt class="col-sm-3">Tên dự án</dt>
                        <dd class="col-sm-9"><strong>@Model.Name</strong></dd>

                        <dt class="col-sm-3">Mô tả</dt>
                        <dd class="col-sm-9">@Model.Description</dd>

                        <dt class="col-sm-3">Ngày bắt đầu</dt>
                        <dd class="col-sm-9">@Model.StartDate.ToString("dd/MM/yyyy")</dd>

                        <dt class="col-sm-3">Ngày kết thúc</dt>
                        <dd class="col-sm-9">@Model.EndDate.ToString("dd/MM/yyyy")</dd>
                    </dl>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Hủy bỏ
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Xác nhận xóa
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
