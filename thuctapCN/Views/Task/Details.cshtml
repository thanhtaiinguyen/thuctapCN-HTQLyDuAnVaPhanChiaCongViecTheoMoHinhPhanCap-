@model thuctapCN.Models.TaskDetailsViewModel

@{
    ViewData["Title"] = "Chi tiết công việc";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4 class="mb-0">
                            <i class="bi bi-file-text"></i> Chi tiết công việc
                        </h4>
                        @if (Model.IsOverdue)
                        {
                            <span class="badge bg-danger">Quá hạn</span>
                        }
                    </div>
                </div>
                <div class="card-body">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show">
                            <i class="bi bi-check-circle"></i> @TempData["SuccessMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show">
                            <i class="bi bi-exclamation-triangle"></i> @TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <h5 class="text-primary">@Model.TaskName</h5>
                            <p class="text-muted mb-1">
                                <i class="bi bi-folder"></i> Dự án: <strong>@Model.ProjectCode - @Model.ProjectName</strong>
                            </p>
                        </div>
                        <div class="col-md-6 text-md-end">
                            @if (Model.Priority == "Cao")
                            {
                                <span class="badge bg-danger fs-6">🔴 Cao</span>
                            }
                            else if (Model.Priority == "Trung bình")
                            {
                                <span class="badge bg-warning text-dark fs-6">🟡 Trung bình</span>
                            }
                            else
                            {
                                <span class="badge bg-success fs-6">🟢 Thấp</span>
                            }
                        </div>
                    </div>

                    <hr />

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <p><strong>Người được giao:</strong></p>
                            <p class="ms-3">
                                <i class="bi bi-person-circle"></i> @Model.AssignedToUserName
                                <br />
                                <small class="text-muted">Mã NV: @Model.AssignedToEmployeeCode</small>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <p><strong>Deadline:</strong></p>
                            <p class="ms-3">
                                <i class="bi bi-calendar-event"></i> @Model.Deadline.ToString("dd/MM/yyyy")
                                @{
                                    var daysLeft = (Model.Deadline.Date - DateTime.Now.Date).Days;
                                    if (daysLeft < 0)
                                    {
                                        <br /><small class="text-danger">Quá hạn @Math.Abs(daysLeft) ngày</small>
                                    }
                                    else if (daysLeft == 0)
                                    {
                                        <br /><small class="text-warning">Hôm nay là deadline!</small>
                                    }
                                    else if (daysLeft <= 2)
                                    {
                                        <br /><small class="text-warning">Còn @daysLeft ngày</small>
                                    }
                                    else
                                    {
                                        <br /><small class="text-muted">Còn @daysLeft ngày</small>
                                    }
                                }
                            </p>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <p><strong>Trạng thái:</strong></p>
                            <p class="ms-3">
                                @if (Model.Status == "Hoàn thành")
                                {
                                    <span class="badge bg-success fs-6">✓ Hoàn thành</span>
                                }
                                else if (Model.Status == "Đang thực hiện")
                                {
                                    <span class="badge bg-primary fs-6">⟳ Đang thực hiện</span>
                                }
                                else if (Model.Status == "Tạm dừng")
                                {
                                    <span class="badge bg-secondary fs-6">⏸ Tạm dừng</span>
                                }
                                else
                                {
                                    <span class="badge bg-light text-dark fs-6">○ Chưa bắt đầu</span>
                                }
                            </p>
                        </div>
                        <div class="col-md-6">
                            <p><strong>Tiến độ:</strong></p>
                            <div class="progress ms-3" style="height: 30px;">
                                <div class="progress-bar @(Model.Progress == 100 ? "bg-success" : "bg-info")" 
                                     role="progressbar" 
                                     style="width: @Model.Progress%"
                                     aria-valuenow="@Model.Progress" 
                                     aria-valuemin="0" 
                                     aria-valuemax="100">
                                    <strong>@Model.Progress%</strong>
                                </div>
                            </div>
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.Description))
                    {
                        <div class="mb-3">
                            <p><strong>Mô tả:</strong></p>
                            <div class="ms-3 p-3 bg-light rounded">
                                @Model.Description
                            </div>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.Notes))
                    {
                        <div class="mb-3">
                            <p><strong>Ghi chú/Báo cáo:</strong></p>
                            <div class="ms-3 p-3 bg-light rounded">
                                @Model.Notes
                            </div>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.AttachmentPath))
                    {
                        <div class="mb-3">
                            <p><strong>File đính kèm:</strong></p>
                            <p class="ms-3">
                                <a href="@Model.AttachmentPath" target="_blank" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-download"></i> Tải xuống file
                                </a>
                            </p>
                        </div>
                    }

                    <hr />

                    <div class="row">
                        <div class="col-md-6">
                            <small class="text-muted">
                                <i class="bi bi-clock-history"></i> Ngày tạo: @Model.CreatedDate.ToString("dd/MM/yyyy HH:mm")
                            </small>
                        </div>
                        <div class="col-md-6 text-md-end">
                            @if (Model.UpdatedDate.HasValue)
                            {
                                <small class="text-muted">
                                    <i class="bi bi-arrow-repeat"></i> Cập nhật lần cuối: @Model.UpdatedDate.Value.ToString("dd/MM/yyyy HH:mm")
                                </small>
                            }
                        </div>
                    </div>
                </div>
                <div class="card-footer">
                    <div class="d-flex justify-content-between">
                        <div>
                            <a asp-action="Index" asp-route-projectId="@Model.ProjectId" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại
                            </a>
                            <a asp-action="ViewReports" asp-route-taskId="@Model.Id" class="btn btn-outline-info ms-2">
                                <i class="bi bi-file-earmark-text"></i> Xem báo cáo
                            </a>
                        </div>
                        <div>
                            @if (Model.CanUpdateStatus)
                            {
                                <a asp-action="CreateReport" asp-route-taskId="@Model.Id" class="btn btn-warning me-2">
                                    <i class="bi bi-send"></i> Gửi báo cáo
                                </a>
                                <a asp-action="UpdateStatus" asp-route-id="@Model.Id" class="btn btn-success me-2">
                                    <i class="bi bi-arrow-repeat"></i> Cập nhật trạng thái
                                </a>
                            }
                            @if (Model.CanEdit)
                            {
                                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                                    <i class="bi bi-pencil"></i> Sửa
                                </a>
                            }
                        </div>
                    </div>
                </div>

                <!-- Comments Section -->
                <div class="card mt-4">
                    <div class="card-header bg-light">
                        <h5 class="mb-0">
                            <i class="bi bi-chat-dots"></i> Bình luận & Trao đổi
                            @if (ViewBag.Comments != null)
                            {
                                <span class="badge bg-secondary">@ViewBag.Comments.Count</span>
                            }
                        </h5>
                    </div>
                    <div class="card-body">
                        @if (ViewBag.CanComment == true)
                        {
                            <!-- Add Comment Form -->
                            <form asp-action="AddComment" asp-controller="Task" method="post" class="mb-4">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="taskId" value="@Model.Id" />
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Thêm bình luận mới</label>
                                    <textarea name="content" class="form-control" rows="3" 
                                              placeholder="Nhập bình luận của bạn..." required maxlength="2000"></textarea>
                                    <small class="form-text text-muted">
                                        <i class="bi bi-info-circle"></i> Tối đa 2000 ký tự
                                    </small>
                                </div>
                                <button type="submit" class="btn btn-primary">
                                    <i class="bi bi-send"></i> Gửi bình luận
                                </button>
                            </form>
                            <hr />
                        }
                        
                        <!-- Comments List -->
                        @if (ViewBag.Comments != null && ViewBag.Comments.Count > 0)
                        {
                            @foreach (var comment in ViewBag.Comments)
                            {
                                <div class="comment-item mb-3 p-3 border rounded bg-light" id="comment-@comment.Id">
                                    <div class="d-flex align-items-start">
                                        <div class="me-3">
                                            @if (!string.IsNullOrEmpty(comment.UserAvatar))
                                            {
                                                <img src="@comment.UserAvatar" alt="Avatar" class="rounded-circle" width="40" height="40" style="object-fit: cover;" />
                                            }
                                            else
                                            {
                                                <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center" 
                                                     style="width: 40px; height: 40px;">
                                                    <i class="bi bi-person"></i>
                                                </div>
                                            }
                                        </div>
                                        <div class="flex-grow-1">
                                            <div class="d-flex justify-content-between align-items-start mb-2">
                                                <div>
                                                    <strong>@comment.UserName</strong>
                                                    <br />
                                                    <small class="text-muted">
                                                        <i class="bi bi-clock"></i> @comment.CreatedDate.ToString("dd/MM/yyyy HH:mm")
                                                        @if (comment.UpdatedDate != null)
                                                        {
                                                            <span class="badge bg-secondary ms-2">Đã chỉnh sửa</span>
                                                        }
                                                    </small>
                                                </div>
                                                @if (comment.CanEdit || comment.CanDelete)
                                                {
                                                    <div class="btn-group btn-group-sm" role="group">
                                                        @if (comment.CanEdit)
                                                        {
                                                            <button type="button" class="btn btn-outline-primary" 
                                                                    onclick="editComment(@comment.Id, '@comment.Content')">
                                                                <i class="bi bi-pencil"></i> Sửa
                                                            </button>
                                                        }
                                                        @if (comment.CanDelete)
                                                        {
                                                            <button type="button" class="btn btn-outline-danger" 
                                                                    onclick="deleteComment(@comment.Id)">
                                                                <i class="bi bi-trash"></i> Xóa
                                                            </button>
                                                        }
                                                    </div>
                                                }
                                            </div>
                                            <div class="comment-content" id="content-@comment.Id">
                                                <p class="mb-0">@comment.Content</p>
                                            </div>
                                            
                                            <!-- Edit Form (hidden by default) -->
                                            <div class="edit-form-@comment.Id" style="display: none;">
                                                <form asp-action="EditComment" asp-controller="Task" method="post">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="commentId" value="@comment.Id" />
                                                    <input type="hidden" name="taskId" value="@Model.Id" />
                                                    <div class="mb-2">
                                                        <textarea name="content" class="form-control" rows="3" required maxlength="2000" 
                                                                  id="edit-content-@comment.Id">@comment.Content</textarea>
                                                    </div>
                                                    <div class="btn-group btn-group-sm">
                                                        <button type="submit" class="btn btn-success">
                                                            <i class="bi bi-check"></i> Lưu
                                                        </button>
                                                        <button type="button" class="btn btn-secondary" 
                                                                onclick="cancelEdit(@comment.Id)">
                                                            <i class="bi bi-x"></i> Hủy
                                                        </button>
                                                    </div>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="text-center py-4">
                                <i class="bi bi-chat-quote" style="font-size: 3rem; color: #ccc;"></i>
                                <p class="text-muted mt-2">Chưa có bình luận nào. Hãy là người đầu tiên!</p>
                            </div>
                        }
                    </div>
                </div>

                <!-- Delete Comment Form (hidden) -->
                <form id="deleteCommentForm" asp-action="DeleteComment" asp-controller="Task" method="post" style="display: none;">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="commentId" id="deleteCommentId" />
                    <input type="hidden" name="taskId" value="@Model.Id" />
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function editComment(commentId, currentContent) {
            // Hide content and show edit form
            document.getElementById('content-' + commentId).style.display = 'none';
            document.querySelector('.edit-form-' + commentId).style.display = 'block';
        }

        function cancelEdit(commentId) {
            // Show content and hide edit form
            document.getElementById('content-' + commentId).style.display = 'block';
            document.querySelector('.edit-form-' + commentId).style.display = 'none';
        }

        function deleteComment(commentId) {
            if (confirm('Bạn có chắc chắn muốn xóa bình luận này?')) {
                document.getElementById('deleteCommentId').value = commentId;
                document.getElementById('deleteCommentForm').submit();
            }
        }

        // Auto-scroll to comments if there's a success/error message about comments
        @if (TempData["SuccessMessage"]?.ToString().Contains("bình luận") == true || 
             TempData["ErrorMessage"]?.ToString().Contains("bình luận") == true)
        {
            <text>
            document.addEventListener('DOMContentLoaded', function() {
                var commentsSection = document.querySelector('.card:has(.bi-chat-dots)');
                if (commentsSection) {
                    commentsSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
            </text>
        }
    </script>
}
